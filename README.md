# Run instruction

## Install

npm install

## Run from root

Then to run from the root folder where the server.js is located, do:

node server.js

open <http://localhost:3000/> in a browser (the port is defined in server.js)
